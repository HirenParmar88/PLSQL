CREATE TABLE STUDENT1(
	SNO NUMBER(3) CONSTRAINTS PKSNO PRIMARY KEY,
	NAME VARCHAR2(20) NOT NULL,
	COURSE VARCHAR2(10) DEFAULT 'BCA'
);

CREATE TABLE FEES2(
	
	AMOUNT NUMBER(3),
	FEESDATE DATE,
	SNO NUMBER(3), CONSTANTS FKSNO FOREIGN KEY(SNO) REFERENCES STUDENT1
);